# Question Service Environment Configuration

# App Settings
APP_NAME=peerprep-question-service
ENV=dev
HOST=localhost
PORT=8003

# DATABASE - Docker PostgreSQL (host.docker.internal for Docker Desktop)
DATABASE_URL=postgresql+asyncpg://peerprep_user:admin@host.docker.internal:5434/peerprep_questions
TEST_DATABASE_URL=postgresql+asyncpg://peerprep_user:admin@host.docker.internal:5434/peerprep_questions_test

# User Service API Configuration
USER_SERVICE_URL=http://localhost:8001

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:3001"]

# User Service API Configuration
USER_SERVICE_URL=http://localhost:8001

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:3001"]