# Question Service Environment Configuration

# App Settings
APP_NAME=peerprep-question-service
ENV=dev
HOST=localhost
PORT=8003

# DATABASE - PostgreSQL
DATABASE_URL=postgresql://peerprep:peerprep@localhost:5432/peerprep_questions_dev
TEST_DATABASE_URL=postgresql://peerprep:peerprep@localhost:5432/peerprep_questions_dev_test

# DATABASE - PostgreSQL for production (uncomment when ready)
# DATABASE_URL=postgresql://username:password@localhost:5432/peerprep_questions
# TEST_DATABASE_URL=postgresql://username:password@localhost:5432/peerprep_questions_test

# JWT (for validating tokens from user service)
JWT_SECRET=secretkey
JWT_ALG=HS256

# AWS (for future deployment)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001