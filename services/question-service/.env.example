# Question Service Environment Configuration

# App Settings
APP_NAME=peerprep-question-service
ENV=dev
HOST=localhost
PORT=8003

# DATABASE - Docker PostgreSQL (host.docker.internal for Docker Desktop)
DATABASE_URL=postgresql://peerprep:peerprep@host.docker.internal:5434/peerprep_questions
TEST_DATABASE_URL=postgresql://peerprep:peerprep@host.docker.internal:5434/peerprep_questions_test

# User Service API Configuration
USER_SERVICE_URL=http://localhost:8001

# AWS (for future deployment)
AWS_REGION=us-east-1

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:3001"]

# DATABASE - Local Development (uncomment to use local PostgreSQL instead)
# DATABASE_URL=postgresql://peerprep:peerprep@localhost:5432/peerprep_questions_dev
# TEST_DATABASE_URL=postgresql://peerprep:peerprep@localhost:5432/peerprep_questions_test

# User Service API Configuration
USER_SERVICE_URL=http://localhost:8001

# AWS (for future deployment)
AWS_REGION=us-east-1

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:3001"]