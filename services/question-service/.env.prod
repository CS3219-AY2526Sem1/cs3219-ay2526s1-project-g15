# Production Environment Configuration
ENV=prod

# App Settings
APP_NAME=peerprep-question-service
HOST=0.0.0.0
PORT=8003

# Database Settings (Question Service - Isolated Database)
# Use environment variables or secrets management in production
DB_HOST=${POSTGRES_HOST}
DB_PORT=${POSTGRES_PORT:-5432}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_NAME=peerprep_questions

# Production databases: peerprep_questions_prod, peerprep_questions_prod_test
# DATABASE_URL will be constructed automatically or use explicit:
# DATABASE_URL=${DATABASE_URL}

# User Service API Configuration
USER_SERVICE_URL=http://localhost:8001

# CORS (restrict to production domains)
ALLOWED_ORIGINS=${ALLOWED_ORIGINS}